pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the application l Maven'
            }
        }
        stage('Unit and Integration Tests') {
            steps {
                echo 'Running unit and integration tests l Selenium'
                emailext body: 'Unit and Integration Test Results',
                    subject: 'Test Stage Status',
                    to: 'henry.and.ruth@gmail.com'
            }
        }
        stage('Code Analysis') {
            steps {
                echo 'Performing code analysis l Checkstyle'
            }
        }
        stage('Security Scan') {
            steps {
                echo 'Performing security scan l Nessus'
                emailext body: 'Security Scan Results',
                    subject: 'Test Stage Status',
                    to: 'henry.and.ruth@gmail.com'
            }
        }
        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to staging server l AWS EC2 instance'
            }
        }
        stage('Integration Tests on Staging') {
            steps {
                echo 'Running integration tests on staging l JUnit'
            }
        }
        stage('Deploy to Production') {
            steps {
                echo 'Deploying to production server l AWS EC2 instance'
            }
        }
    }
}
