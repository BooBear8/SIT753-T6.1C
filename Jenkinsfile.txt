pipeline {
    agent any 

    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                echo 'Tool: Maven or similar build automation tool' 
            }
        }

        stage('Unit and Integration Tests') {
            steps {
                echo 'Running unit and integration tests...'
                echo 'Tools: JUnit, Mockito, Selenium, or similar testing frameworks' 
            }
        }

        stage('Code Analysis') {
            steps {
                echo 'Performing code analysis...'
                // Assuming SonarQube generates an HTML report in the 'reports' directory
                sh 'sonar-scanner -Dsonar.projectKey=myproject -Dsonar.sources=. -Dsonar.host.url=http://your-sonarqube-server -Dsonar.login=your-token'
            }
        }

        stage('Security Scan') {
            steps {
                echo 'Conducting security scan...'
                echo 'Tool: OWASP ZAP or similar security scanning tool'
                // Assuming OWASP ZAP generates a report in the 'zap_reports' directory
                sh 'zap-cli quick-scan --self-contained --spider http://your-app-url --report-html zap_reports/zap-report.html'
            }
            post {
                success {
                    mail to: 'henry.and.ruth@gmail.com',
                        subject: 'Security Scan: Successful',
                        body: 'The security scan has completed successfully.'
                }
            }
        }

        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to staging server...'
                echo 'Tool: Ansible, Jenkins Deploy Plugin, or similar deployment automation tool'
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                echo 'Running integration tests on staging...'
                echo 'Tools: Same as Unit and Integration Tests stage'
            } 
            post {
                success {
                    mail to: 'henry.and.ruth@gmail.com',
                        subject: 'Integration Tests on Staging: Successful',
                        body: 'The integration tests on staging have completed successfully.'
                }
            }
        }

        stage('Deploy to Production') {
            steps {
                echo 'Deploying to production server...'
                echo 'Tool: Ansible, Jenkins Deploy Plugin, or similar deployment automation tool' 
            }
        }
    }
    post {
        always { 
            emailext body: '''
                ${SCRIPT, template="groovy-html.template"}
                <pre>
                    ${BUILD_LOG, maxLines=500, escapeHtml=false} 
                </pre>
            ''',
            subject: "${env.JOB_NAME} - Build # ${env.BUILD_NUMBER} - ${currentBuild.currentResult}",
            to: 'henry.and.ruth@gmail.com',
            attachmentsPattern: '**/reports/sonar-report.html, **/zap_reports/zap-report.html' // Attach SonarQube and ZAP reports
        }
    }
}